name: Finalize onboarding foundation

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  validate-and-finalize:
    runs-on: ubuntu-latest
    steps:
      - name: Check out feature branch
        uses: actions/checkout@v4
        with:
          ref: feat/onboarding-foundation
          fetch-depth: 0

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 11.20.0
          run_install: false

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.1
          cache: pnpm

      - name: Relax package age gate for temporary validation
        run: pnpm config set minimumReleaseAge 0

      - name: Install dependencies and refresh lockfile
        run: pnpm install --no-frozen-lockfile

      - name: Typecheck
        run: pnpm typecheck

      - name: Export web bundle
        run: pnpm exec expo export --platform web

      - name: Commit validated lockfile and remove this temporary workflow
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pnpm-lock.yaml
          git rm .github/workflows/validate-onboarding-foundation.yml
          git commit -m "chore: validate onboarding foundation"
          git push origin HEAD:feat/onboarding-foundation
