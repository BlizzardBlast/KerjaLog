import { PlaceholderTabScreen } from '@/shared/components/PlaceholderTabScreen';

export default function GrowthRoute() {
  return (
    <PlaceholderTabScreen
      eyebrow="Growth"
      title="Your evidence map"
      description="Skill evidence counts will live here without ratings, scores, or gamification."
    />
  );
}
